Gear repo. 
Async voice assistant (XAVI) that pipelines speech-to-text (Vosk), a local LLM (Ollama), 
and text-to-speech (Piper) with a producer-consumer audio queue — delivering low-latency 
responses by streaming TTS playback while the model is still generating.
